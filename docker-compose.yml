services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: framezero
      POSTGRES_USER: framezero
      POSTGRES_PASSWORD: framezero
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
